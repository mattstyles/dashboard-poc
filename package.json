{
  "name": "dashboard-poc",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "bin": {
    "dashboard-poc": "seed.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm i && ( cd app && npm install && npm run build)",
    "seed": "./bin/seed.js -n 300",
    "start": "npm run setup && ( cd app; npm start )"
  },
  "config": {
    "port": 8989
  },
  "author": "Matt Styles",
  "license": "ISC",
  "dependencies": {
    "minimist": "1.2.0",
    "superagent": "1.8.0"
  }
}
